# PostgreSQL connection settings
PGHOST=localhost
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PGPASSWORD=postgres

# Optional: Full connection string (overrides individual settings above)
# PGCONNSTRING=host=localhost port=5432 dbname=postgres user=postgres password=postgres

# S3 API settings
AWS_S3_PORT=9000 